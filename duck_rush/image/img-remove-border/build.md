# 图片提取工具

生成的脚本放在当前文件所在目录下

## 输入

输入的是手机截图

## 输出

截图中央的图片区域，移除截图周边的空白区域

## 边框检测算法

1. 先选择中心节点
2. 以中心节点往外扩散，检测整行或者整列是否都是纯白色或者纯黑色，如果纯色的连续高度或者连续宽度大于指定像素数量，则说明是边框
3. 根据边框进行裁剪，保留中央的有效区域

## 连续纯色检测算法

引入一个数组 solid_arr，如果遇到纯色就往数组里面append，否则把数组清空，如果数组大小大于阈值 X，说明遇到了边框

